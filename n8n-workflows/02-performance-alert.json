{
  "name": "Performance Alert",
  "description": "Monitors campaign KPIs and sends alerts when thresholds are exceeded",
  "nodes": [
    {
      "name": "Webhook Trigger",
      "type": "n8n-nodes-base.webhook",
      "parameters": {
        "path": "performance-alert",
        "httpMethod": "POST"
      },
      "position": [250, 300]
    },
    {
      "name": "Note",
      "type": "n8n-nodes-base.stickyNote",
      "parameters": {
        "content": "TODO: Add logic to check thresholds and send WhatsApp/Email notifications via Twilio or SMTP."
      },
      "position": [500, 200]
    }
  ],
  "connections": {}
}
